[defaults]
    collections_path = ~/.ansible/collections:./collections
    host_key_checking = false
    gathering = explicit
    inventory = inventory/ath.yaml
    vault_password_file = ~/private/ansible-vault/ansible.pw
